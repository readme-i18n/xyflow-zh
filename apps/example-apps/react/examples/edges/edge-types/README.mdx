---

title: 边类型
description: 将边设置为直线、阶梯线、平滑阶梯线或贝塞尔曲线
---

在 React Flow 中你可以选择不同类型的[边类型](/api-reference/react-flow#edgeTypes)：`default`（贝塞尔曲线）、`straight`（直线）、`step`（阶梯线）和`smoothstep`（平滑阶梯线）。如图所示，你可以为每条边单独定义类型，并在同一图表中混合使用。

<RemoteCodeViewer route="examples/edges/edge-types" framework="react" />