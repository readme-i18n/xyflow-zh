---

title: 拖放删除边
description: 当未找到连接手柄时删除边
---

本示例展示了如何通过使用 [`onReconnect`、`onReconnectStart` 和 `onReconnectEnd` 处理函数](/api-reference/react-flow#onReconnect)来删除边。如果您拖动现有边并将其放置在面板上，该边将从 `edges` 数组中删除。

<RemoteCodeViewer
  route="examples/edges/delete-edge-on-drop"
  framework="react"
/>