---

title: 更新节点
description: 更新特定节点的数据字段
---

你可以自由更新节点和边的属性，只要将新创建的 `nodes` 或 `edges` 数组传递给 `ReactFlow` 即可。

<Callout type="info">
  必须为节点创建新的 `data` 对象，才能通知 React Flow 数据变更。
</Callout>

<br />

<RemoteCodeViewer route="examples/nodes/update-node" framework="react" />