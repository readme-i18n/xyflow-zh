---

description: 
  React Flow 快速入门指南，展示如何安装该包、使用方法及示例应用获取途径
pagination_next: null
---

import { Emoji } from '@xyflow/xy-ui';
import { Callout, Cards, Steps } from 'nextra/components';
import { LiteYouTubeEmbed } from 'xy-shared';
import { RemoteCodeViewer } from 'xy-shared/server';

# 快速入门

本页面将帮助您在几分钟内从零开始构建一个可运行的 React Flow 应用。若您仅想初步了解 React Flow 的功能特性，可体验我们的无代码交互式 [Playground](https://play.reactflow.dev/)。

## 安装

首先，使用您偏好的方式创建新 React 项目——我们推荐使用 [Vite](https://vitejs.dev/)

```bash copy npm2yarn
npm init vite my-react-flow-app -- --template react
```

接着通过 `cd` 命令进入项目目录，添加 [`@xyflow/react`](https://npmjs.com/package/@xyflow/react) 作为依赖项

```bash copy npm2yarn
npm install @xyflow/react
```

最后启动开发服务器即可开始开发！

## 使用指南

我们将渲染来自 `@xyflow/react` 包的 [`<ReactFlow />`](/api-reference/react-flow#reactflow) 组件。只需配合定义若干 [node](/api-reference/types/node) 对象、[edge](/api-reference/types/edge) 对象及 [event handlers](/api-reference/react-flow#event-handlers) 即可实现基础功能！清空 `App.jsx` 文件内容并添加以下代码：

```jsx "import '@xyflow/react/dist/style.css';" "width: '100vw', height: '100vh'"
import { useState, useCallback } from 'react';
import { ReactFlow, applyNodeChanges, applyEdgeChanges, addEdge } from '@xyflow/react';
import '@xyflow/react/dist/style.css';

const initialNodes = [
  { id: 'n1', position: { x: 0, y: 0 }, data: { label: 'Node 1' } },
  { id: 'n2', position: { x: 0, y: 100 }, data: { label: 'Node 2' } },
];
const initialEdges = [{ id: 'n1-n2', source: 'n1', target: 'n2' }];

export default function App() {
  const [nodes, setNodes] = useState(initialNodes);
  const [edges, setEdges] = useState(initialEdges);

  const onNodesChange = useCallback(
    (changes) => setNodes((nodesSnapshot) => applyNodeChanges(changes, nodesSnapshot)),
    [],
  );
  const onEdgesChange = useCallback(
    (changes) => setEdges((edgesSnapshot) => applyEdgeChanges(changes, edgesSnapshot)),
    [],
  );
  const onConnect = useCallback(
    (params) => setEdges((edgesSnapshot) => addEdge(params, edgesSnapshot)),
    [],
  );

  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <ReactFlow
        nodes={nodes}
        edges={edges}
        onNodesChange={onNodesChange}
        onEdgesChange={onEdgesChange}
        onConnect={onConnect}
        fitView
      />
    </div>
  );
}
```

此处需注意两个关键点：

- <Emoji content="🎨" /> 必须导入 CSS 样式表才能使 React Flow 正常工作
- <Emoji content="📐" /> `<ReactFlow />` 组件必须置于具有明确宽高的父元素内

## 运行结果

大功告成！您已成功创建首个交互式流程图！<Emoji content="🎉" />

<RemoteCodeViewer route="learn/quickstart" framework="react" />

## 后续步骤

<Cards>
  <Cards.Card title="核心概念" href="/learn/concepts/terms-and-definitions" />
  <Cards.Card title="自定义配置" href="/learn/customization/custom-nodes" />
  <Cards.Card title="示例项目" href="/examples" />
  <Cards.Card title="API 参考" href="/api-reference" />
  <Cards.Card title="Discord" href="https://discord.gg/RVmnytFmGW" />
  <Cards.Card
    title="模板项目"
    href="https://github.com/xyflow/react-flow-example-apps"
  />
</Cards>