---

description: "该钩子简化了受控流程的原型设计，使您能够在 ReactFlowInstance 外部管理节点和边的状态。可以将其视为带有额外辅助回调的 React `useState` 钩子。"

# useEdgesState()

[GitHub 源码](https://github.com/xyflow/xyflow/blob/main/packages/react/src/hooks/useNodesEdgesState.ts)

该钩子简化了受控流程的原型设计，使您能够在 `ReactFlowInstance` 外部管理节点和边的状态。可以将其视为带有额外辅助回调的 React `useState` 钩子。

```jsx
import { ReactFlow, useNodesState, useEdgesState } from '@xyflow/react';

const initialNodes = [];
const initialEdges = [];

export default function () {
  const [nodes, setNodes, onNodesChange] = useNodesState(initialNodes);
  const [edges, setEdges, onEdgesChange] = useEdgesState(initialEdges);

  return (
    <ReactFlow
      nodes={nodes}
      edges={edges}
      onNodesChange={onNodesChange}
      onEdgesChange={onEdgesChange}
    />
  );
}
```

## 签名

<APIDocs functionName="useEdgesState" />

## TypeScript

该钩子接受自定义边类型的泛型参数。更多信息请参阅[TypeScript指南中的相关章节](/learn/advanced-use/typescript#nodetype-edgetype-unions)。

```tsx
const nodes = useEdgesState<CustomEdgeType>();
```

## 注意事项

- 创建此钩子旨在简化原型设计并使文档示例更清晰。虽然在生产环境中使用此钩子没有问题，但在实践中您可能需要使用更复杂的状态管理方案，例如 [Zustand](/docs/guides/state-management/)。