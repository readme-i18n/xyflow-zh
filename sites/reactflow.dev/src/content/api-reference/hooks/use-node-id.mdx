---

description: "通过此钩子可获取所在节点的ID。当您需要在渲染树深层获取节点ID但不想手动逐层传递prop时特别有用。"
---

# useNodeId()

[Source on Github](https://github.com/xyflow/xyflow/blob/v11/packages/core/src/contexts/NodeIdContext.ts/#L7)

通过此钩子可获取所在节点的ID。当您需要在渲染树深层获取节点ID但不想手动逐层传递prop时特别有用。

```js
import { useNodeId } from '@xyflow/react';

export default function CustomNode() {
  return (
    <div>
      <span>This node has an id of </span>
      <NodeIdDisplay />
    </div>
  );
}

function NodeIdDisplay() {
  const nodeId = useNodeId();

  return <span>{nodeId}</span>;
}
```

## 签名

<APIDocs functionName="useNodeId" />

## 注意事项

- 该钩子仅应在自定义节点或其子组件中使用。