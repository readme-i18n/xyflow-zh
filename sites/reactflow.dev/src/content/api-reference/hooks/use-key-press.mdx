---

description: '该钩子函数允许监听特定键码，并告知当前是否处于按下状态。'
---

# useKeyPress()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/react/src/hooks/useKeyPress.ts)

该钩子函数允许监听特定键码，并告知当前是否处于按下状态。

```jsx
import { useKeyPress } from '@xyflow/react';

export default function () {
  const spacePressed = useKeyPress('Space');
  const cmdAndSPressed = useKeyPress(['Meta+s', 'Strg+s']);

  return (
    <div>
      {spacePressed && <p>Space pressed!</p>}
      {cmdAndSPressed && <p>Cmd + S pressed!</p>}
    </div>
  );
}
```

## 签名

<APIDocs functionName="useKeyPress" />

## 注意事项

- 此钩子不依赖 `ReactFlowInstance`，因此可在应用任意位置自由使用！