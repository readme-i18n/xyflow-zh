---

description: '一个便捷工具，用于通过新属性重新连接现有边。该工具会在边数组中搜索具有匹配ID的边，并使用您提供的连接更新其属性。'

# reconnectEdge()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/core/src/utils/graph.ts/#L97)

一个便捷工具，用于使用新属性更新现有的[`Edge`](/api-reference/types/edge)。该工具会在边数组中搜索具有匹配`id`的边，并使用您提供的连接更新其属性。

```js
const onReconnect = useCallback(
  (oldEdge: Edge, newConnection: Connection) => setEdges((els) => reconnectEdge(oldEdge, newConnection, els)),
  []
);
```

## 签名

<APIDocs functionName="reconnectEdge" />