---

description: '该工具函数返回给定边界的视口。
  可用于在服务器端预计算一组节点的视口，或在不直接改变视口的情况下计算给定边界的视口。'
---

import { Callout } from 'nextra/components';

# getViewportForBounds()

[Source on Github](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/general.ts/#L170)

该工具函数返回给定边界的视口。
可用于在服务器端预计算一组节点的视口，或在不直接改变视口的情况下计算给定边界的视口。

> [!NOTE]
>
> 该函数曾命名为 `getTransformForBounds`

```js
import { getViewportForBounds } from '@xyflow/react';

const { x, y, zoom } = getViewportForBounds(
  {
    x: 0,
    y: 0,
    width: 100,
    height: 100,
  },
  1200,
  800,
  0.5,
  2,
);
```

## 签名

<APIDocs functionName="getViewportForBounds" />

## 注意事项

- 这是一个底层工具函数。如需更实用的 API，可参考 [`fitView`](/api-reference/types/react-flow-instance#fitview) 或 [`fitBounds`](/api-reference/types/react-flow-instance#fitbounds) 方法。