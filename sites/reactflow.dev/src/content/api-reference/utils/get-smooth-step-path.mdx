---

description: 'getSmoothStepPath 工具函数返回渲染两个节点间阶梯路径所需的所有元素。borderRadius 属性可用于控制阶梯拐角的圆滑程度。'
---

# getSmoothStepPath()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/edges/smoothstep-edge.ts/#L215)

`getSmoothStepPath` 工具函数返回渲染两个节点间阶梯路径所需的所有元素。`borderRadius` 属性可用于控制阶梯拐角的圆滑程度。

```js
import { Position, getSmoothStepPath } from '@xyflow/react';

const source = { x: 0, y: 20 };
const target = { x: 150, y: 100 };

const [path, labelX, labelY, offsetX, offsetY] = getSmoothStepPath({
  sourceX: source.x,
  sourceY: source.y,
  sourcePosition: Position.Right,
  targetX: target.x,
  targetY: target.y,
  targetPosition: Position.Left,
});

console.log(path); //=> "M0 20L20 20L 70,20Q 75,20 75,25L 75,95Q ..."
console.log(labelX, labelY); //=> 75, 60
console.log(offsetX, offsetY); //=> 75, 40
```

## 签名

<APIDocs functionName="getSmoothStepPath" />

## 注意事项

- 该函数返回一个元组（即固定大小的数组），以便更轻松地同时处理多条边路径
- 将 `borderRadius` 属性设为 `0` 可获取阶梯状边路径