---

description: '给定一个可能相互连接的节点数组和一个包含所有边的数组，该工具函数会返回连接给定节点中任意节点的边数组。'
---

# getConnectedEdges()

[GitHub 源代码](https://github.com/xyflow/xyflow/blob/main/packages/system/src/utils/graph.ts/#L224)

该工具函数会过滤边数组，仅保留那些源节点或目标节点存在于给定节点数组中的边。

```js
import { getConnectedEdges } from '@xyflow/react';

const nodes = [
  { id: 'a', position: { x: 0, y: 0 } },
  { id: 'b', position: { x: 100, y: 0 } },
];
const edges = [
  { id: 'a->c', source: 'a', target: 'c' },
  { id: 'c->d', source: 'c', target: 'd' },
];

const connectedEdges = getConnectedEdges(nodes, edges);
// => [{ id: 'a->c', source: 'a', target: 'c' }]
```

## 签名

<APIDocs functionName="getConnectedEdges" />