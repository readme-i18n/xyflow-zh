---

title: ViewportPortal 组件
sidebarTitle: '<ViewportPortal />'
description: 'ViewportPortal 组件可用于将组件添加到与节点和边相同的视口流中渲染。当您需要渲染与节点和边共享同一坐标系，并受缩放和平移操作影响的自定义组件时，该组件非常有用'
created_at: 2024-02-21
---

# &lt;ViewportPortal />

[GitHub 源码](https://github.com/xyflow/xyflow/blob/main/packages/react/src/components/ViewportPortal/index.tsx)

`<ViewportPortal />` 组件可用于将组件添加到与节点和边相同的视口流中进行渲染。当您需要渲染与节点和边共享同一坐标系，并受缩放和平移操作影响的自定义组件时，该组件非常实用。

```jsx
import React from 'react';
import { ViewportPortal } from '@xyflow/react';

export default function () {
  return (
    <ViewportPortal>
      <div
        style={{ transform: 'translate(100px, 100px)', position: 'absolute' }}
      >
        This div is positioned at [100, 100] on the flow.
      </div>
    </ViewportPortal>
  );
}
```

## Props 属性

<APIDocs componentName="ViewportPortal" />