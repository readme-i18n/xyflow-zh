---

title: NodeResizer 组件
sidebarTitle: '<NodeResizer />'
description: 'NodeResizer 组件可为节点添加尺寸调整功能，它会在节点周围渲染可拖拽的控制点，支持全方位缩放'
---

import { RemoteCodeViewer } from 'xy-shared/server';

# \<NodeResizer />

[GitHub 源码](https://github.com/xyflow/xyflow/blob/main/packages/react/src/additional-components/NodeResizer/NodeResizer.tsx)

`<NodeResizer />` 组件可为节点添加尺寸调整功能，它会在节点周围渲染可拖拽的控制点，支持全方位缩放。

```jsx
import { memo } from 'react';
import { Handle, Position, NodeResizer } from '@xyflow/react';

const ResizableNode = ({ data }) => {
  return (
    <>
      <NodeResizer minWidth={100} minHeight={30} />
      <Handle type="target" position={Position.Left} />
      <div style={{ padding: 10 }}>{data.label}</div>
      <Handle type="source" position={Position.Right} />
    </>
  );
};

export default memo(ResizableNode);
```

## Props 属性

TypeScript 用户可通过 `NodeResizerProps` 类型获取该组件的属性定义。

<APIDocs componentName="NodeResizer" />

## 使用示例

前往 [示例页面](/examples/nodes/node-resizer) 查看具体实现。

<RemoteCodeViewer route="examples/nodes/node-resizer" framework="react" />

### 自定义缩放控制

如需构建自定义缩放控制组件，可使用 [NodeResizeControl](/api-reference/components/node-resize-control) 组件进行定制化开发。

## 注意事项

- 参考 [`NodeProps`](/api-reference/types/node-props) 类型文档
- 或查阅 [自定义节点](/learn/customization/custom-nodes) 指南了解如何实现专属节点