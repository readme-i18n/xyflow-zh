---

description:
  ResizeParams 类型用于定义由 NodeResizer 组件触发的各类事件类型。有时您会看到该类型还扩展了额外的 direction 字段。
---

# ResizeParams

[Source on Github](https://github.com/xyflow/xyflow/blob/v11/packages/node-resizer/src/types.ts/#L4)

`ResizeParams` 类型用于定义由 `<NodeResizer />` 组件触发的各类事件类型。有时您会看到该类型还扩展了额外的 direction 字段。

## 字段说明

<APIDocs typeName="ResizeParams" />