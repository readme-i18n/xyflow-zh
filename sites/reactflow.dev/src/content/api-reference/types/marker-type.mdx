---

description: 边线(Edge)的两端可选择性地添加标记。MarkerType 类型枚举了配置标记时可用的选项。
---

# MarkerType

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/system/src/types/edges.ts/#L82-L83)

边线(Edge)的两端可选择性地添加标记。MarkerType 类型枚举了配置标记时可用的选项。

```ts
export enum MarkerType {
  Arrow = 'arrow',
  ArrowClosed = 'arrowclosed',
}
```